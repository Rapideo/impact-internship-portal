// Re-export — ConfirmModal's canonical home is app/components/ConfirmModal.tsx
// (existing call sites import from there). Re-exported here for forward
// compatibility with the plan's preferred modal/ namespace.
export { ConfirmModal, type ConfirmModalProps } from '../ConfirmModal';
