// Re-export — EmptyRow's canonical home is app/components/EmptyRow.tsx
// (existing call sites import from there). Re-exported here for forward
// compatibility with the plan's preferred tables/ namespace.
export { EmptyRow } from '../EmptyRow';
