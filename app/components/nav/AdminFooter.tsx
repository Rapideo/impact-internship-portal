// Re-export — the canonical AdminFooter lives at app/components/AdminFooter.tsx.
// Re-exported here so callers using the new nav/ path resolve cleanly.
export { AdminFooter } from '../AdminFooter';
