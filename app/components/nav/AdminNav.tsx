// Re-export — the canonical AdminNav lives at app/components/AdminNav.tsx and
// is consumed by the admin layout. The plan's preferred path is this nav/
// directory, so re-export here for forward-compatibility.
export { AdminNav, type AdminNavProps } from '../AdminNav';
